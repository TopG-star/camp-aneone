# ─────────────────────────────────────────────────────────────
# .env.test.local.template — Test environment overrides
# Copy to .env.test.local, fill in values, NEVER commit
# These are loaded only during test runs.
# See docs/SETUP_ENV.md for instructions.
# ─────────────────────────────────────────────────────────────

NODE_ENV=test
PORT=4001
DATABASE_PATH=:memory:
NEXTAUTH_SECRET=test-secret-do-not-use-in-production
API_TOKEN=test-token-do-not-use-in-production
ALLOWED_EMAILS=test@test.com
LOG_LEVEL=error
