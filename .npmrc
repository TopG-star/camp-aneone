# Hoist all packages to root node_modules.
# Required for Docker builds: pnpm's default isolated linker creates symlinks
# into the virtual store that break inside Docker image layers.
# With hoisting, all packages land in root node_modules/ — no per-package
# node_modules needed, identical behaviour to npm/yarn in Docker.
shamefully-hoist=true
